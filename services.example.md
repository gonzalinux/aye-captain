## Apps & Services

| App name (as it appears in logs) | GitHub repo | Notes |
|-----------------------------------|-------------|-------|
| my-app | my-org/my-app | Short description, language/framework |
| my-other-app | my-org/my-other-app | Short description |

## Deploys

List the deploy labels used in your VictoriaLogs streams:

- `production` — main production environment
- `staging` — staging environment

## Stream Labels

Any extra VictoriaLogs stream labels specific to your setup (beyond the defaults: `app`, `deploy`, `level`, `node`, `filename`):

- `region` — e.g. eu-west, us-east
